import styled from "styled-components";
export const FileUploadButton = styled.input`
  color: transparent;
  input[type="file"] {
    opacity: 0;
    color: rgba(0, 0, 0, 0);
    color: transparent;
  }
`